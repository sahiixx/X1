// PostCSS is not currently used by the build (src/index.css is pre-compiled).
// Kept for when the project switches to source-driven Tailwind.
export default {
  plugins: {
    autoprefixer: {},
  },
};